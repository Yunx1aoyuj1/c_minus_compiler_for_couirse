/* Syntax error - Missing ')'
*/

void main(void)
{

    int x;
    int div;
    int rem;
 
    x = input();

    while (x > 0)
    {
       div = x/10;
       rem = x-(div*10;
       output(rem);
       x = div;
    }
}
