//------ test control flow statements -------

void main(void) {
  int x;
  int y;
  int z;

  // test control flow
  if (x == 1) 
    { z = 1; }
  else
    { z = 2; }

  while (x < 6) 
    { x = x + 1; }

  // test relational & logical expressions

  if ( (x == y)  ) 
    { z = 5; }

  if ( (x != z) ) 
    { y = 2; }
}

